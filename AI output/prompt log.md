# AI 对话记录


## 1

好的，现在我需要生成一份报告，用于指导我的vibe coding 的工作和后续的论文撰写。

我有一个核心的观察：
1. 在遥感图像中，船只目标的尺寸很小，但是纹理特征明显且密集；尾迹的尺寸很大（bbox size)，但是特征稀疏。
2. 船只目标和尾迹目标存在几何位置上的联系，因此可以互相作为先验知识，作为对下一层的注意力的初始蒙版。

我需要基于 sm3det 的基础上增加（或者修改）3个模块，作为我的创新点设计：
1. 我修改了多专家模块 grid level MoE，将针对多模态的特意化构造改为了对于单模态的不同特征的目标的检测；
2. 我新增了残差连接 residual block，和 grid level MoE 模块并行，将全局信息引入到后一层。考虑到船只是稠密特征，而尾迹是稀疏特征，每一次下采样，都会先提取船只信息，然后是尾迹。因此我的残差连接上的变换（条带卷积，低频滤波）是交错设置的。我的backbone 共计4层。
3. 我改进了backbone新增了 Dual Stream 的管线设置。图像每经过一层，都会对称的生成注意力蒙版mask，用于指导下一层的网络。船只的蒙版用于引导尾迹，尾迹的则用于引导船只。

我和 sm3det的区别在于：
1. 我从多模态降级回了单一模态，但我沿用了 多专家 MoE 的思路；
2. 我从若干种类的目标，降级回了2种目标：
3. 船只、尾迹；多任务的检测头改为了2种目标的检测头（一个头检测船只，一个头检测尾迹，和后续的Dynamic Submodule Optimization (DSO)模块串联起来）

接下来我的工作计划：
1. 在本地利用 kimi code 辅助理解项目，修改代码，最终整合得到repo，push到github上。
2. 在auto-DL 的容器部署环境，下载数据集，然后测试数据通路，确保所有的模块输入输出是对齐的，我需要一个很小的数据集（10张图片）就够（我最终测试所需的数据集已经找好了，是swimship）
3. 在我提出的Dual Stream 管线模块的输出端，加一个函数，可以输出这一步的中间图像和注意力蒙版，用于后续调试和观察效果
4. 全量的数据集放入训练和测试，后续优化和调试。


## 2

我是一名研究生，我的课题方向是：遥感图像领域的目标识别。现在我准备完成我的第一批论文的写作，我的任务是：光学遥感图像中的船只尾迹目标检测。
我使用的基座模型是 SM3Det，我使用的数据集是开源的swimshipwake-imagery-mass。
接下来，我需要你帮我解读和分析  SM3Det 这一篇论文。然后我会告诉你，我的模型的修改的构想，你来评价可行性并补充细节，最后，我需要使用 kimi code 辅助我的代码编程任务，完成这一篇论文的代码工作。

SM3Det 这一篇论文的核心点在于，将 遥感 领域的3类常见模态串联了起来，也就是 optical infrared and SAR，他使用了多专家模块 MoE和一个router，多任务(HBB, OBB)的下游检测头，和一个Dynamic Submodule Optimization (DSO)可以动态差异化的调节每个检测头的学习率。

而我的任务是：光学图像中的船只目标检测，因此是单模态，2种指定的目标类型，同时是单任务（oriented bounding box detection, OBB)，我也需要使用差异化的学习率模块。同时，更进一步的，我希望设计一个Dual stream 的管线构造。

我有一个核心的观察：
1. 在遥感图像中，船只目标的尺寸很小，但是纹理特征明显且密集；尾迹的尺寸很大（bbox size)，但是特征稀疏。
2. 船只目标和尾迹目标存在几何位置上的联系，因此可以互相作为先验知识，作为对下一层的注意力的初始蒙版。

我需要基于 sm3det 的基础上增加（或者修改）3个模块，作为我的创新点设计：
1. 我修改了多专家模块 grid level MoE，将针对多模态的特意化构造改为了对于单模态的不同特征的目标的检测；
2. 我新增了残差连接 residual block，和 grid level MoE 模块并行，将全局信息引入到后一层。考虑到船只是稠密特征，而尾迹是稀疏特征，每一次下采样，都会先提取船只信息，然后是尾迹。因此我的残差连接上的变换（条带卷积，低频滤波）是交错设置的。我的backbone 共计4层。
3. 我改进了backbone新增了 Dual Stream 的管线设置。图像每经过一层，都会对称的生成注意力蒙版mask，用于指导下一层的网络。船只的蒙版用于引导尾迹，尾迹的则用于引导船只。

我和 sm3det的区别在于：
1. 我从多模态降级回了单一模态，但我沿用了 多专家 MoE 的思路；
2. 我从若干种类的目标，降级回了2种目标：
3. 船只、尾迹；多任务的检测头改为了2种目标的检测头（一个头检测船只，一个头检测尾迹，和后续的Dynamic Submodule Optimization (DSO)模块串联起来）

接下来我的工作计划：
1. 在本地利用 kimi code 辅助理解项目，修改代码，最终整合得到repo，push到github上。
2. 在auto-DL 的容器部署环境，下载数据集，然后测试数据通路，确保所有的模块输入输出是对齐的，我需要一个很小的数据集（10张图片）就够（我最终测试所需的数据集已经找好了，是swimship）
3. 在我提出的Dual Stream 管线模块的输出端，加一个函数，可以输出这一步的中间图像和注意力蒙版，用于后续调试和观察效果
4. 全量的数据集放入训练和测试，后续优化和调试。

附件中的pdf是 sm3det 的论文。现在，我需要你完成如下任务，请你基于遥感目标检测领域相关的知识，和当下图像处理模型中的常见处理手法：
1. 解读这篇论文，核心创新点是什么？
2. 这篇论文迎合了当下哪些热门的tag，哪些概念是单词为了渲染而创造出来的，哪些模块是直接引用的已有的成熟论文的，哪些是真正有原创性工作的。
3. 这篇论文中有提到哪些训练和硬件配置的细节？

然后，完成如下任务：
1. 考虑我的课题方向（光学单一模态，尾迹目标检测，带旋转的框），你会怎么修改这一篇论文并构造出3个创新点模块？
2. 考虑我提出的构想，请进行评价，并补充更多的细节，例如（残差连接中使用什么变换更加合理？）
3. 生成一份用于指导 kimi code 的报告。






